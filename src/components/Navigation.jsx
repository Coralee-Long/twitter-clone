import NavIconsList from "./NavIconsList";
import icons from "../utils/icons.json";

const Navigation = () => {
  return (
    <>
      <NavIconsList icons={icons} />
    </>
  );
};

export default Navigation;
